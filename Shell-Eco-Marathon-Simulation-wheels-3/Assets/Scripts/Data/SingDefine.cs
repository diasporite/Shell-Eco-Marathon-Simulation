using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SingDefine : MonoBehaviour
{
    public SingDefine specific;
    // Start is called before the first frame update
    void Start()
    {
        specific = this; 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
